#include <stdio.h>
#include <conio.h>


int main()
{
		int i,n;
		printf("%5s%10s%20s%10s%10s\n", "Qty", "ISBN", "Title", "Price","Total" );
		for(int i=0;i<n;i++){
		printf("%5d%10d%20s%10d%10d\n", s[i].Qty, s[i].ISBN, s[i].Title, s[i].Price,s[i].Total );
		}
		return 0;
}
